namespace Task2
{
    internal struct MainWord
    {
        /*public string Word { get; set; }

        public MainWord(ILanguage language, MyConsole myConsole)
        {
            do
            {
                myConsole.Clear();
                language.EnterMainWord();
                this.Word = myConsole.ReadMessage();

            } while (!Regex.IsMatch(this.Word, language.regex));
        }

        public string ReturnMainWord()
        {
            Console.WriteLine(this.MainWord);
        }*/

        /*public bool ReadPlayerWord(ILanguage language, MyConsole myConsole, string mainWord)
        {
            string playerWord;
            do
            {
                myConsole.Clear();
                language.EnterPlayerWord(this.Name);
                playerWord = myConsole.ReadMessage();

            } while (!Regex.IsMatch(playerWord, language.regex));
            Console.WriteLine(playerWord);
            return true;
        }*/
    }
}

